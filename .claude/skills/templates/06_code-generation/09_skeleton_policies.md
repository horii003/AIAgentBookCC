# スケルトン: ビジネスルール・ポリシー (agent_knowledge/{domain}_policies.py)

## 概要

専門エージェントが参照するビジネスルール・ポリシーをテキストとして返却するモジュール。
ドメインごとにファイルを作成し、業務ルールをマークダウン形式で定義する。

**重要**: このモジュールは他のモジュールに依存しない（純粋なテキスト返却のみ）。

## ファイル配置

`agent_knowledge/{domain_a}_policies.py`（ドメインAの場合）
`agent_knowledge/{domain_b}_policies.py`（ドメインBの場合）

## スケルトンコード

```python
"""
{ドメインA}ポリシー

このモジュールは、{ドメインA}エージェントが使用するルールテキストを提供します。
"""


def get_{domain_a}_rules(**kwargs) -> str:
    """
    {ドメインA}のルールテキストを取得

    このルールは、{ドメインA}処理時に適用される必須チェック項目を定義します。

    Args:
        **kwargs: ルールテキストに埋め込む動的パラメータ

    Returns:
        str: ルールのテキスト（マークダウン形式）
    """
    # TODO: 詳細設計書に従い実装
    # - 業務ルールのチェック項目をマークダウン形式で返却する
    # - 動的パラメータが必要な場合はkwargsから取得してf-stringで埋め込む
    pass
```


## カスタマイズガイド

1. **ドメインごとのファイル作成**: 専門エージェントごとに対応するポリシーファイルを作成する
2. **チェック項目の定義**: 業務ルールに応じてチェック項目を定義する
3. **関数の引数**: 動的パラメータが不要な場合は引数なしの関数でもよい
4. **段階的な対処手順**: 単純な却下ではなく、修正提案 → 理由確認 → 最終判断の流れを定義する
