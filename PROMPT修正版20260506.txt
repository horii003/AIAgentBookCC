■
ワークフローに従って、以下の要求事項を元に業務要件定義フェーズの成果物を作成してください。
要求事項から読み取れない要件未定義項目が残っていてもそのまま進めてください。
対象フェーズ: 01_business-requirements

要求事項: 
社内には「経費精算申請」「出張申請」「交通費精算申請」「稟議申請」など、様々な申請があります。
どういう場合にどこにどのような申請を出せばよいか分かりにくい課題があります。
社員が「出張したい」「備品を購入したい」などの申請内容を入力すると、
社内の申請ルールを参照して必要な申請種別を判断し、使用すべき申請書と申請先を提示するAIエージェントを開発します。
申請ルールや申請方法が分からない利用者でも正しく申請できることを目的とします。
エージェントは不足情報を対話で確認しながら申請書を自動作成し、申請内容をチェックし、申請ミスや差し戻しを減らします。

■
# 役割
あなたはAIエージェント開発の業務要件定義を専門とする設計者です。

# 指示
以下の内容で作成済の業務要件定義（artifacts/01_business-requirements/outputs）を修正してください。

# 修正内容
## 1. 対象申請種別の限定
本システムは以下の2種類の申請のみを対象とします：
- 交通費精算申請
- 経費精算申請

### 申請種別の判断不能ケース
申請内容から交通費精算申請・経費精算申請のいずれかを判断できない場合は、
ユーザーに申請種別を確認します。
- ユーザーに「交通費精算申請」「経費精算申請」の選択肢を提示する
- ユーザーの選択に従って対応する申請フローへ誘導する

## 2. ターゲットユーザー
社内の一般社員（申請ルールや申請方法に詳しくない場合がある）

## 3. 申請対象
### 経費精算申請の対象経費区分
- 事務用品費（書籍、文房具、オフィス用品など）
- 宿泊費（ホテル、宿泊施設など）
- 資格精算費（資格試験、受験料、認定費用など）
- その他経費（上記以外の業務関連経費）

### 交通費精算申請の対象交通手段
- 電車
- バス
- タクシー
- 飛行機

## 4. 交通費精算申請の業務ルール
- 移動情報（移動日・出発地・目的地・交通手段・費用）を一区間ずつ収集
- 対応交通手段: 電車、バス、タクシー、飛行機の4種類
- 運賃データから交通費を自動計算（電車は経路テーブル検索、バス/タクシー/飛行機は固定運賃）
- 申請は経費発生日から90日以内であること
- 交通費が10,000円を超える場合は事前に上長の承認が必要
- 駅名の正規化（「渋谷駅」→「渋谷」）
- すべての申請に業務目的の記載が必要


## 5. 経費精算申請の業務ルール

- 領収書画像から店舗名、金額、日付、品目を自動抽出
- 品目から経費区分を自動判断: 事務用品費、宿泊費、資格精算費、その他経費の4区分
- 申請は経費発生日から90日以内であること
- 経費が5,000円を超える場合は事前に上長の承認が必要
- すべての申請に業務目的の記載が必要

## 6. 申請例

- 「タクシー代を精算したい」→ 交通費精算申請
- 「電車で移動した交通費を精算したい」→ 交通費精算申請
- 「文房具を購入した領収書を精算したい」→ 経費精算申請
- 「ホテルの宿泊費を精算したい」→ 経費精算申請

## 7. 申請書名

- 交通費精算申請書
- 経費精算申請書

# 受入基準

- 作成後に以下の受入基準を満たしているか確認すること
  - 修正した内容と異なる内容が設計書内に無いこと
- 作成後にフェーズ品質チェックの「残存する未定義項目（要件上未定義）」を更新すること

■
前作業（再作成の場合）
システム要件定義フェーズ以降の成果物を削除して再作成します。
以下の作業をお願いします。

1. workflow-state.md の システム要件定義フェーズ以降のチェックをすべて外し、現在のステージを「業務要件定義 完了」に戻す。

対象フェーズ: 02_system-requirements
削除する成果物:
02_system-requirements/outputs フォルダ以下のファイルと02_system-requirements/reviews フォルダ以下のファイル
03_system-design/outputs フォルダ以下のファイルと03_system-design/reviews フォルダ以下のファイル
04_basic-design/outputs フォルダ以下のファイルと04_basic-design/reviews フォルダ以下のファイル
05_detailed-design/outputs フォルダ以下のファイルと05_detailed-design/reviews フォルダ以下のファイル

作成
ワークフローに従って、システム要件定義フェーズの成果物を作成してください。業務要件定義フェーズ成果物の要件未定義は、そのままで進めてください。


■
# 役割

あなたはAIエージェント開発のシステム要件定義を専門とする設計者です。

# 指示

以下の内容で作成済のシステム要件定義書（artifacts/02_system-requirements/outputs）を修正してください。ただしシステム要件定義書以外は更新しないでください。

# 修正内容

## 1. エージェント構成

- エージェントは、申請受付窓口エージェント、交通費精算申請エージェント、経費精算申請エージェントの3つ。
- チェックエージェント廃止を受けて、各チェック機能は他の各専門エージェントに責務を移行する

## 2. 申請業務の基本方針

- 従業員が業務遂行のために立て替えた経費について、領収書を基に精算申請を行う
  - 領収書は画像で提供される
- AIエージェントが行う申請作業は、Excelファイルで申請書を作成することを目的とする
- 申請受付窓口エージェントがまず申請者名、次に申請内容を受け取り、申請内容からユーザーの意図を分類し、適切な専門エージェントに自動振り分けする
- 対話を通じた段階的な情報収集を行う
- Excel生成前にHuman-in-the-Loop承認（OK / 修正 / キャンセル）が必要

## 3. 申請に必要な情報

### 交通費精算申請

- 申請者名
- 申請日
- 各区間の移動情報
  - 移動日
  - 出発地
  - 目的地
  - 交通手段
  - 費用
  - 業務目的

### 経費精算申請

- 申請者名
- 申請日
- 経費情報
  - 購入日 
  - 店舗名
  - 品目
  - 経費区分
  - 金額
  - 業務目的
  - 領収書（証憑）

## 4. 申請書テンプレートのセル位置定義

- 申請書は各申請書のテンプレートを使用して生成する

### 交通費精算申請書

- 申請者名：B3、申請日：B4
- 移動区間（No/日付/出発地/目的地/交通手段/費用/業務目的/承認状況）：A{7+i}〜H{7+i}（iは、0～n-1で、nは件数）
- 合計金額：H{7+n+2}

### 経費精算申請書

- 申請者名：B3、申請日：B4
- 経費（No/購入日/店舗名/品目/経費区分/金額/業務目的/承認状況）：A{7+i}〜H{7+i} （iは、0～n-1で、nは件数）
- 合計金額：H{7+n+2}

## 5. 運賃テーブルのデータ定義変更

- 形式を「CSV/Excel」から「JSON」に変更する
- 運賃データは役割の異なる2ファイル構成とする
   - 電車の経路・運賃データ（出発駅・到着駅・運賃を保持）
   - バス（230円）・タクシー（10,000円）・飛行機（50,000円）の固定運賃データ（交通手段ごとの固定金額を保持）
- 2ファイルそれぞれをデータ一覧に個別レコードとして登録する

## 6. 申請期限チェックの責務定義

- 申請期限チェック（経費発生日から3ヶ月以内）は、交通費精算申請エージェントおよび経費精算申請エージェントの責務とする。申請受付窓口エージェントの責務ではない

## 7. 機能ツール構成

- エージェントで使用するツールは、交通費計算ツールと申請書生成ツールの2つ

## 8. ユーザー入力文字数制限

- CLIからのユーザー入力に対して、文字数制限を500文字とする
- 500文字を超えた場合、入力が長すぎる旨を通知し再入力を促す

## 9. 対話回数上限

- 対話回数上限を30回とする

## 10. セッション管理要件
- セッションストアにメモリや揮発性ストレージは使用しない
- セッション管理のresume要件として、セッション情報が存在する場合は復元可能とする

## 11. データ管理・ナレッジ管理要件
- データの永続化方法としてRDBは使用しない。ファイル永続化を使用する。
- ナレッジベースとしてRAGを使用しない。ファイルを使用する。

## 12.評価テスト
- 受付エージェントが申請内容を正しく分類し、適切な専門エージェントに振り分けていることを継続的に検証することを目的とし
- 評価指標一覧をツール選択精度とゴール達成率 を評価指標とする。ツール選択精度とゴール達成率 以外の評価指標を削除する。

# 受入基準

- 作成後に以下の受入基準を満たしているか確認すること
  - 修正した内容と異なる内容が設計書内に無いこと
- 作成後にフェーズ品質チェックの「残存する未定義項目（要件上未定義）」を更新すること



■
前作業（再作成の場合）
システム設計フェーズ以降の成果物を削除して再作成します。
以下の作業をお願いします。

・workflow-state.md の システム設計フェーズ以降のチェックをすべて外し、現在のステージを「システム要件 完了」に戻す

対象フェーズ: 03_system-design
削除する成果物:
03_system-design/outputs フォルダ以下のファイルと03_system-design/reviews フォルダ以下のファイル
04_basic-design/outputs フォルダ以下のファイルと04_basic-design/reviews フォルダ以下のファイル
05_detailed-design/outputs フォルダ以下のファイルと05_detailed-design/reviews フォルダ以下のファイル

作成
ワークフローに従って、システム設計フェーズの成果物を作成してください。上流フェーズの要件未定義は、そのままで進めてください。
システム設計の前提となる技術スタックとアーキテクチャ方針は、以下の通りです。

対象フェーズ: 03_system-design

# 技術スタック

## 言語とランタイム

- Python 3.x
- エントリーポイント: `main.py`

## コアフレームワーク

- AWS Strands Agents (`strands-agents`, `strands-agents-tools`, `strands-agents-builder`) — マルチエージェント・オーケストレーションフレームワーク Version 1.25.0
- エージェントはLLMバックエンドとしてAmazon Bedrock（Claude Sonnet 4.5）を使用
- モデル設定は `config/model_config.py` に集約

## 主要ライブラリ

- `pydantic` (v2+) — すべての入力と状態モデルのデータバリデーション
- `openpyxl` — 経費申請書のExcelファイル生成
- `boto3` / `botocore` — Bedrockアクセス用AWS SDK
- `Pillow` — 領収書読み取り用の画像処理（`strands_tools` の `image_reader` が内部で使用する間接依存）
- `python-dotenv` — 環境変数管理
- `python-dateutil` — 日付解析

## ナレッジ・外部連携の実現方式

- 申請ルールナレッジ: システムプロンプト埋め込み（RAG不使用）
- 領収書OCR: `strands_tools` の `image_reader` を直接使用

# アーキテクチャ方針

## マルチエージェント構成

- 実装方式: Agent As Tools
- 構成: 階層型マルチエージェント
  - 申請受付窓口エージェント（オーケストレーター）
  - 交通費精算申請エージェント（専門エージェント）
  - 経費精算申請エージェント（専門エージェント）

## テスト
- `pytest` — テストランナー（マーカー: `unit`, `integration`, `slow`, `llm`）
- `hypothesis` — プロパティベーステスト
- `pytest-cov` — カバレッジレポート
## よく使うコマンド
```bash
# 依存関係のインストール
pip install -r requirements.txt
# アプリケーションの実行
python main.py
# すべてのテストを実行
pytest
# カバレッジ付きでテストを実行
pytest --cov=agents --cov=tools --cov=handlers --cov-report=term-missing
# ユニットテストのみ実行
pytest -m unit
# 統合テストのみ実行
pytest -m integration
```
## 環境設定
- `.env.template`を`.env`にコピーし、AWS認証情報を設定
- AWSリージョンのデフォルトは`ap-northeast-1`
- ログレベルは`LOG_LEVEL`環境変数で設定可能（デフォルト: WARNING）
- ログは`logs/agent.log`と標準出力に書き込まれます

■
# 役割

あなたはAIエージェント開発のシステム設計を専門とする設計者です。

# 指示

以下の内容で作成済のシステム設計（artifacts/03_system-design/outputs）を修正してください。ただしシステム設計書以外は更新しないでください。

# 修正内容

## 1. アーキテクチャ

### ウィンドウサイズ

- ウィンドウサイズは申請受付窓口エージェントは30、交通費精算申請エージェントは20、経費精算申請エージェントは15とする

### 実行環境

- ローカルPC上でCLIを使用してエージェントと対話する

### データ管理

- データはPCの所定のフォルダにファイルで格納する

## 2. ReActループ制御

- 最大回数: 全エージェント共通 10回
- 実装クラス: `LoopControlHook`（HookProvider として実装）

### 登録イベント

- BeforeInvocationEvent（エージェント呼び出し開始時にループカウンタを0にリセット）
- AfterModelCallEvent（LLM呼び出し後にループカウンタをインクリメントし上限到達を監視）
- AfterInvocationEvent（エージェント呼び出し完了後にループカウンタを0にリセット）

## 3. Human-in-the-Loop

- Excel生成前のHuman-in-the-Loop承認（OK/修正/キャンセルの3択）

## 4. Bedrock API リトライ設定

- Bedrock API自動リトライ（最大6回、初期遅延4秒、最大遅延240秒、指数バックオフ）

## 5. バリデーション

- Pydantic v2モデルによる全入出力データの集中バリデーション
- 日付形式: YYYY-MM-DD
- 運賃データ: JSONファイルからの静的読み込み

## 6. invocation_state フィールド仕様

### オーケストレーター → 専門エージェント（ToolContext経由）

- applicant_name（申請者名）
- application_date（申請日）
- session_id（セッションID）

### 専門エージェント内部でエージェントに渡す invocation_state

- applicant_name（申請者名）
- application_date（申請日）

> **注意事項**
> - `session_id` はオーケストレーターから専門エージェントへの受け渡しには含めるが、専門エージェント内部では、セッションマネージャーの初期化に直接使用し、エージェントへの invocation_state には含めない
> - invocation_state は辞書リテラルで渡す。専用の Pydantic モデルは定義しない
> - `session_id` はツール関数の引数に含めない

## 7. 申請者情報の取得タイミング

- 申請者名はユーザーとの対話で収集するのではなく、アプリケーション起動時（対話ループ開始前）に取得し、エージェントの初期化パラメータとして渡す
- 申請日はユーザーとの対話で収集するのではなく、システム日付（実行時の日付、YYYY-MM-DD形式）を自動取得する

## 8. ログ設計方針

- ログ設定（レベル・出力先・フォーマット）はエントリーポイントで一元管理する
- ログ出力は各モジュールが責任を持って行う
- ログレベルは環境変数（LOG_LEVEL）で制御し、未設定時のデフォルトは WARNING とする
- ユーザー向けメッセージと技術ログは分離する
- 各モジュールが付加するコンテキスト情報の詳細は基本設計・詳細設計で定義する
- 設計判断を記録

## 9. エラー伝播の責務分担

- 各モジュール（エージェント・ツール）：例外発生箇所でログを出力し、ErrorHandler へ処理を委譲する
- ErrorHandler：ユーザー向けメッセージを生成して呼び出し元に返す
- 呼び出し元：受け取ったメッセージを戻り値または表示として上位レイヤーへ伝播させる

## セッション管理
- セッションの保存先はdata/sessions/
- 保持期間：セッション終了時に削除


# 受入基準

- 作成後に以下の受入基準を満たしているか確認すること
  - 修正した内容と異なる内容が設計書内に無いこと
- 作成後にフェーズ品質チェックの「残存する未定義項目（要件上未定義）」を更新すること


■
前作業（再作成の場合）
基本設計フェーズ以降の成果物を削除して再作成します。
以下の作業をお願いします。

・workflow-state.md の 基本設計フェーズ以降のチェックをすべて外し、現在のステージを「システム設計 完了」に戻す

対象フェーズ: 04_basic-design
削除する成果物:
04_basic-design/outputs フォルダ以下のファイルと04_basic-design/reviews フォルダ以下のファイル
05_detailed-design/outputs フォルダ以下のファイルと05_detailed-design/reviews フォルダ以下のファイル

作成
ワークフローに従って、基本設計フェーズの成果物を作成してください。上流フェーズの要件未定義は、そのままで進めてください。
対象フェーズ: 04_basic-design

■
# 役割

あなたはAIエージェント開発の基本設計を専門とする設計者です。

# 指示

以下の内容で作成済の基本設計書（artifacts/04_basic-design/outputs）を修正してください。ただし基本設計書以外は更新しないでください。

# 修正内容

## 1. 対話ループ

### 特殊コマンド

- 会話履歴と申請者情報のリセットを行うコマンドを追加する
- 対象コマンド: `reset` / `リセット` / `最初から`

## 2. エージェント設計

### ツール割り当て方針

- 各エージェントが使用するツールは、業務機能（計算・生成など）に特化したツールのみとする

### 交通費精算申請エージェント／経費精算申請エージェント 共通

#### プロンプト構成要素

- エラー発生時の振る舞い（呼び出し元エージェントへの要約メッセージ返却）を含める

#### エラー対応方針

- システム系エラー（ツールエラー・システム障害等）の対応方針: 呼び出し元エージェントに要約して返す

#### 申請書ドラフト生成

- 収集済み申請情報をテキストとして整理・提示する（ツール呼び出しなし）
- ドラフト提示ステップとツール呼び出しステップを明確に分離し、Human-in-the-Loop 承認が1回のみ発生するようにする
- Human-in-the-Loop承認（HumanApprovalHook）は BeforeToolCallEvent でトリガーし、申請者の承認（OK）後にはじめて申請書生成ツールを実行する
- 承認前にツールを実行してはならない
- 上記のルールはフロー図・フロー制御の考え方・ハンドラー設計・ツール設計・テスト観点の全箇所で統一して記載すること

## 3. ツール設計

### 申請書生成ツール


- 出力ファイルパスはツール内部で実行時タイムスタンプをもとに自律的に生成する
  - 形式: `datetime.now().strftime("%Y%m%d%H%M%S")`
  - LLM のパラメータ（ツール関数の引数）として受け取ってはならない

### 交通費計算ツール

#### 入力パラメータ追加

- 移動日（日付、YYYY-MM-DD形式、必須）を追加する
  - 申請期限チェック（経費発生日から3ヶ月以内）に使用するため必須とする

# 受入基準

- 作成後に以下の受入基準を満たしているか確認すること
  - 修正した内容と異なる内容が設計書内に無いこと
- 作成後にフェーズ品質チェックの「残存する未定義項目（要件上未定義）」を更新すること



■
前作業（再作成の場合）
詳細設計以降の成果物を削除して再作成します。
以下の作業をお願いします。

1. workflow-state.md の 詳細設計以降のチェックをすべて外し、現在のステージを「基本設計 完了」に戻す。

対象フェーズ: 05_detailed-design
削除する成果物: 05_detailed-design/outputs フォルダ以下のファイルと05_detailed-design/reviews フォルダ以下のファイル


作成
ワークフローに従って、詳細設計フェーズの成果物を作成してください。上流フェーズの要件未定義は、そのままで進めてください。
対象フェーズ: 05_detailed-design

■以下の作業を分割して並列で実行してください。

# 役割

あなたはAIエージェント開発の詳細設計を専門とする設計者です。

# 指示

以下の内容で作成済の詳細設計書（artifacts/05_detailed-design/outputs）を修正してください。

# 修正内容

## 1. 固定運賃JSON ファイル構造

```json
{
  "交通手段A": 金額,
  "交通手段B": 金額,
  "交通手段C": 金額
}
```

## 2. Human-in-the-Loop 承認フック

### 承認コールバック仕様

#### シグネチャ

- 引数: `tool_name`（str）、`tool_params`（dict）
- 戻り値: `tuple[bool, str]`

#### 戻り値

- `(True, "")` → OK（何もしない → ツール実行される）
- `(False, "修正内容")` → 修正要望（`event.cancel_tool` にメッセージをセット → ツールキャンセル）
- `(False, "CANCEL")` → キャンセル（`event.cancel_tool` にキャンセルメッセージをセット → ツールキャンセル）

#### ツール中止方法

- `event.stop_reason` ではなく `event.cancel_tool` にメッセージ文字列をセットする

> 設計書内でこの仕様に関連する記述（シグネチャ・戻り値・処理フロー・コード例・テスト観点等）をすべて洗い出し、上記仕様と一致するよう修正すること。

## 3. 申請受付窓口エージェント

### ウェルカムメッセージ（出力メッセージに追加）

```
============================================================
こちらは申請受付窓口エージェントです
社内の様々な申請作業をサポートします

最初に申請者名を入力してください。その後、申請したい内容をお知らせください。キーワードでも構いません

※終了するには 'exit' または 'quit' と入力ください
※最初からやり直すには 'reset' と入力ください
============================================================
```

### 対話ループ 入力プロンプト表示文言

```
\n\n入力内容（終了時はquit）: 
```

## 4. 申請書 列定義

### 交通費精算申請書 移動区間明細

- A: No（行番号）
- B: 移動日
- C: 出発地
- D: 目的地
- E: 交通手段
- F: 費用
- G: 業務目的
- H: 承認状況（初期値：空欄）

### 経費精算申請書 経費明細

- A: No（行番号）
- B: 購入日
- C: 店舗名
- D: 品目
- E: 経費区分
- F: 金額
- G: 業務目的
- H: 承認状況（初期値：空欄）

## 5. 妥当性チェックルールの参照先変更

妥当性チェックルールをインラインで記述するのではなく、エージェント初期化時に、外部ファイルを参照しプロンプトに組み込む。

### 交通費精算申請エージェント

- ルール参照先: `agent_knowledge/transportation_policies.py`
- 移動情報の収集方式は、1区間ずつ対話で収集する。1区間の情報は入力内容を一括で情報提示する。

### 経費精算申請エージェント

- ルール参照先: `agent_knowledge/receipt_policies.py`
- 領収書画像読み取りは strands_tools の image_reader を使用する

## 6. LoopControlHook 仕様

### ループ上限到達時の処理

- 上限到達時は `raise LoopLimitError` でエラーを発生させる
- カウントは `AfterModelCallEvent` で行うが、`event.exception` が存在する場合はスキップする

### LoopLimitError の定義場所

- `LoopLimitError` はカスタム例外クラスとして定義すること
- 定義するモジュールを設計書内で明示し、依存関係・使用例・テスト観点など全セクションで統一して記載すること

### ログ出力イベント（ループ制御用イベントに加え、以下のログ出力用イベントも登録する。）

- BeforeModelCallEvent: ループ回数を INFO レベルでログ出力
- BeforeToolCallEvent: ツール名を INFO レベルでログ出力
- AfterToolCallEvent: ツール名を INFO レベルでログ出力

### その他仕様

- AfterInvocationEvent のハンドラーでは「合計ループ回数のログ出力（INFO）」を行う。（ループカウンタのリセットは行わない）
- BeforeInvocationEvent のハンドラーではループカウンタのリセットは行う
- LoopLimitError は current_iteration・max_iterations・agent_name の3フィールドを保持する
- LoopLimitErrorを raise する場合、定義済みの全フィールドを引数として渡すこと
- ループ上限到達時のログレベルは WARNING とする
- テンプレートファイルのパスは data/templates/を使用する
- 出力ディレクトリは data/output/{session_id}/を使用する

## 7. ErrorHandler クラス仕様

- クラスのメソッドの責務は「ユーザー向けメッセージ文字列の生成と返却」のみとする
- クラスの責務にログ出力・セッション状態更新を含めない
- コンストラクタの引数は持たない（SessionManager 等の依存注入は行わない）
- コンストラクタに `logger` インスタンス変数を定義しない
- 外部ライブラリ依存として `logging` を列挙しない
- 全メソッドの引数は `例外オブジェクト` のみとする
- 各メソッドの戻り値は「ユーザー向けメッセージ文字列（str）」のみとする
- 各メソッド内でのセッション状態更新（SessionManager の呼び出し）は禁止する
- 各メソッド内でのログ出力（logging.error / logging.warning 等）は禁止する
- ログ出力は呼び出し元モジュール（各エージェント・各ツール）が ErrorHandler 呼び出し前に実施する

### メソッド仕様

- ErrorHandlerメソッド定義を、以下の仕様にする。
  - handle_throttling_error: 対象例外 ModelThrottledException、用途 APIレート制限
  - handle_max_tokens_error: 対象例外 MaxTokensReachedException、用途 最大トークン数到達
  - handle_context_window_error: 対象例外 ContextWindowOverflowException、用途 コンテキストウィンドウ超過
  - handle_fare_data_error: 対象例外 FileNotFoundError / Exception、用途 運賃データ読み込み失敗
  - handle_calculation_error: 対象例外 Exception、用途 運賃計算失敗
  - handle_file_save_error: 対象例外 IOError / PermissionError / Exception、用途 Excelファイル保存失敗
  - handle_validation_error: 対象例外 ValidationError、用途 Pydanticバリデーション失敗
  - handle_keyboard_interrupt: 対象例外 KeyboardInterrupt、用途 ユーザーによる中断
  - handle_loop_limit_error: 対象例外 LoopLimitError、用途 ループ上限到達
  - handle_runtime_error: 対象例外 RuntimeError、用途 その他の実行時エラー
  - handle_unexpected_error: 対象例外 Exception、用途 予期しないエラー

## 8. HumanApprovalHook 仕様

- 承認対象ツール名は 申請書生成ツール詳細設計書に定義された全ツール関数名とする

## 9. ツール共通: 申請者名・申請日の取得

- 申請者名・申請日は LLM がツールパラメータとして渡す値ではなく、ツール関数内部で invocation_state から取得する値とする
- 対象ツール: 交通費計算ツール、申請書生成ツール


## 10. 交通費計算ツール データファイルパス

- 電車経路・運賃データ: `/data/train_routes.json`
- 固定運賃データ: `/data/fixed_fares.json`
- JSONデータ構造: routes キーあり・ネスト形式（現行 train_fares.json と同様）を維持
- JSONファイルの検索は リスト線形探索を使用する

## 11. 交通費計算ツール エラー時戻り値

- データ読み込み系メソッドのエラー時戻り値は `tuple[bool, str]` 型の `(False, エラーメッセージ文字列)` とする
- 計算系メソッドのエラー時戻り値は `{"success": False, "message": エラーメッセージ文字列}` の dict 形式とする
- 戻り値形式は、以下の全セクションで統一して記載すること
  - 例外テーブル（発生条件・戻り値列）
  - エラー処理フロー（フロー図内の戻り値）
  - エラーメッセージ詳細テーブル
  - テスト観点（異常系テスト）
- 例外を raise せず、必ず戻り値としてエラー情報を返す（補足情報・実装上の注意点セクションも統一した記載をすること）
- FileNotFoundError のログレベルは WARNING とする

## 12. 申請書生成ツール エラー時戻り値

- ファイル保存メソッドのエラー時戻り値は `(False, エラーメッセージ文字列)` のタプル形式とする
- 申請書生成メソッド（`generate()`）のエラー時戻り値は `{"success": False, "message": エラーメッセージ文字列}` の dict 形式とする
- 戻り値形式は、以下の全セクションで統一して記載すること
  - 例外テーブル（発生条件・戻り値列）
  - エラー処理フロー（フロー図内の戻り値）
  - エラーメッセージ詳細テーブル
  - テスト観点（異常系テスト）
- 全エラーログに `file_path` を付加情報として含める

## 13. ツールのエラー時例外クラス定義

### 交通費計算ツール（fare_tools.py）

- 経路テーブルに経路が存在しない場合の例外クラスは `ValueError` とする（`KeyError` は使用しない）

### 申請書生成ツール（excel_generator.py）

- ファイル保存メソッドの例外クラスは `IOError`・`PermissionError`・`Exception` の3種類を個別に定義する（`IOError` を `PermissionError` に統合しない）
- 申請書生成メソッド（`generate()`）の例外クラスは `ValidationError` と `Exception` の2種類のみとする（`FileNotFoundError` は定義しない）

## 14. 申請受付窓口エージェントの例外捕捉

以下の例外を個別に捕捉し、ErrorHandler でユーザー向けメッセージを生成して print で表示する。
- `KeyboardInterrupt`：INFO レベルでログ出力。ループを終了する（break）
- `LoopLimitError`：WARNING レベルでログ出力。ループを継続する（continue）
- `ContextWindowOverflowException`：WARNING レベルでログ出力。ループを継続する（continue）
- `MaxTokensReachedException`：WARNING レベルでログ出力。ループを継続する（continue）
- `RuntimeError`：ERROR レベル（exc_info=True）でログ出力。ループを継続する（continue）
- `Exception`（その他）：ERROR レベル（exc_info=True）でログ出力。ループを継続する（continue）
- 戻り値はなし（対話ループのため不要）
- ログにはセッションIDを付加情報として含める
- 上記の継続/終了の区別は、エラーハンドリングテーブル・ログ出力テーブル・テスト観点の全箇所で統一して記載すること

## 15. 専門エージェントの例外捕捉

以下の例外を個別に捕捉し、それぞれエラーメッセージ文字列を返す。
- `LoopLimitError`：WARNING レベルでログ出力
- `ContextWindowOverflowException`：WARNING レベルでログ出力
- `MaxTokensReachedException`：WARNING レベルでログ出力
- `RuntimeError`：ERROR レベル（exc_info=True）でログ出力
- `Exception`（その他）：ERROR レベル（exc_info=True）でログ出力
- エラー時の戻り値はすべて `str`（呼び出し元 LLM コンテキストに渡されるため）
- ログにはクエリの先頭 50 文字を付加情報として含める

## 16. ファイル読み込み実装方針

- ファイル存在チェックは `os.path.exists()` による事前チェックで実装すること
- `open()` 実行時の `FileNotFoundError` 捕捉との併用は禁止し、事前チェックを優先する

## 17. セッションID 生成方法

- session_id の形式・採番ルールを以下のとおり定義する
  - 形式: {タイムスタンプ（秒単位）}_{UUID（8文字）}
  - 例: 20260323153045_a1b2c3d4
  - 生成方法: datetime.now().strftime("%Y%m%d%H%M%S") + "_" + uuid.uuid4().hex[:8]
  - 生成タイミング: セッション開始時にAG-001が生成する

# 受入基準

- 作成後に以下の受入基準を満たしているか確認すること
  - 修正した内容と異なる内容が設計書内に無いこと
- 作成後にフェーズ品質チェックの「残存する未定義項目（要件上未定義）」を更新すること


■
前作業（再作成の場合）
実装以降の成果物を削除して再作成します。
以下の作業をお願いします。
・workflow-state.md の 実装フェーズ以降のチェックをすべて外し、現在のステージを「詳細設計 完了」に戻す
・task.md の 実装以降のチェックをすべて外す。

対象フェーズ: 06-code-generation
削除する成果物: 06-code-generation/src フォルダ以下のファイル


作成
ワークフローに従って、実装フェーズの成果物を作成してください。
対象フェーズ: 06-code-generation



■
main.pyを使って開発したAIエージェントを実際に起動して、エンドツーエンドのテストで、申請書ファイルが作成されるところまでを動作確認してください。

