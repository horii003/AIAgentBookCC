---
version: "1.0.0"
last_updated: "2026-05-02"
updated_by: ""
---


# [評価テスト名] 評価テスト詳細設計書 

> **参照元（システム要件定義資料）:**
> - 評価指標一覧.md（評価指標一覧）

> **参照元（システム設計資料）:**
> - 評価テスト共通設計.md 
> - 共通設定方針.md
> - マルチエージェント連携設計.md

> **参照元（基本設計資料）:**
> - [エージェント名]基本設計書.md
> - [ツール名]詳細設計書.md


## 1. 概要



---

## 2. モジュール構成

### 2.1 インポート一覧

| モジュール | 用途 |
|---|---|
| `[モジュール名]` | [用途] |

---

## 3. 初期設定処理

ファイル先頭のモジュールレベルで以下を順に実行する。

### 3.1 標準入出力のエンコーディング設定

[エンコーディング設定の仕様を記載]

### 3.2 sys.path の設定

[sys.path設定の仕様を記載]

### 3.3 環境変数の読み込み

[環境変数読み込みの仕様を記載]

### 3.4 フック設定

[フック設定の仕様を記載（不要な場合は「なし」と記載）]

---

## 4. ログ設定

### 4.1 設定内容

| 設定項目 | 値 |
|---|---|
| ログレベル | [レベル] |
| フォーマット | `[フォーマット文字列]` |
| 出力先1 | [出力先] |
| 出力先2 | `[ファイルパス]` |
| ファイルエンコーディング | [エンコーディング] |

### 4.2 SDK ログの抑制

| ロガー名 | 設定レベル |
|---|---|
| `[ロガー名]` | [レベル] |

---

## 5. テストケース定義（EVAL_CASES）

### 5.1 Case オブジェクトの構造

```python
Case(
    name="<ケース識別子>",
    input="<ユーザー入力>",
    metadata={
        "task_description": "<人間向けの説明>",
        "[評価器固有のキー]": "<評価基準となる値>",
    },
)
```

### 5.2 ケース一覧

#### TC-001: [ケース名]

| 項目 | 内容 |
|---|---|
| `name` | `[ケース識別子]` |
| `input` | `[ユーザー入力]` |
| `task_description` | [説明] |
| `[評価器固有のキー]` | `[値]` |
| **検証観点** | [観点] |

---

## 6. タスク関数（run_eval_task）

### 6.1 シグネチャ

```python
def run_eval_task(case: Case) -> dict:
```

| 戻り値キー | 型 | 説明 |
|---|---|---|
| `output` | `str` | [説明] |
| `trajectory` | `Session` | [説明] |

### 6.2 処理フロー

```
run_eval_task(case)
  │
  ├── [1] [処理内容]
  ├── [2] [処理内容]
  └── [N] return {"output": [最終応答], "trajectory": [session]}
```

### 6.3 実行方式

[シングルターン / マルチターン の選択と根拠]

### 6.4 ログ出力

| タイミング | レベル | メッセージ形式 |
|---|---|---|
| ケース開始時 | INFO | `[メッセージ]` |

---

## 7. メイン処理（main 関数）

### 7.1 処理フロー

```
main()
  │
  ├── [1] [タイトル表示]
  ├── [2] evaluator = [評価器クラス名](model=get_model())
  ├── [3] experiment = Experiment(cases=EVAL_CASES, evaluators=[evaluator])
  ├── [4] reports = experiment.run_evaluations(run_eval_task)
  └── [5] for report in reports: [レポート保存・表示]
```

### 7.2 レポート保存設定

| 設定 | 値 | 理由 |
|---|---|---|
| `ensure_ascii` | [True/False] | [理由] |
| `indent` | [値] | [理由] |
| エンコーディング | [エンコーディング] | [理由] |

---

## 8. 出力ファイル仕様

| ファイルパス | 種別 | 内容 |
|---|---|---|
| `evals/logs/[ファイル名].log` | 実行ログ | [内容] |
| `evals/logs/[ファイル名]_report.json` | 評価レポート | [内容] |

### 8.1 レポート JSON の構造（概略）

```json
{
  "cases": [
    {
      "name": "[ケース名]",
      "input": "[入力]",
      "output": "[出力]",
      "evaluations": [
        {
          "evaluator": "[評価器名]",
          "score": "[スコア]",
          "reasoning": "[判定根拠]"
        }
      ]
    }
  ]
}
```

---

## 9. エラーハンドリング・制約事項

### 9.1 エラーハンドリング

[エラーハンドリング方針を記載（フォールバック処理がある場合はその条件と処理内容を記載）]

### 9.2 制約事項

| 制約 | 内容 |
|---|---|
| [制約名] | [内容] |

---

## 11. 変更履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| YYYY-MM-DD | 2.0 | 新フォーマットで作成 |
| - | 1.0 | 初版（旧フォーマット） |
